
//
#include<climits>
#include<cstdlib>
#include<iostream>
#define MAX_SIZE 5

using std::cin;
using std::cout;

void readNumbers(int numbers[], int size)
{ 
    for(int I = 0; I < size; I++)
    {
        int number = 0;
        cout << "Enter number at index " << I << ":";
        cin >> number; 
        numbers[I] = number; 
    } 
}

int findHighest(int numbers[], int size)
{ 
    int highestNumber = INT_MIN; // <climits>
    
    for(int I = 0; I < size; I++)
    {
        if(numbers[I] > highestNumber)
        {
            highestNumber = numbers[I];
        } 
    } 

    return highestNumber;
}

void findHighestTest()
{
    int size = MAX_SIZE; //int size {10};
    int x[size] {}; 
     
    readNumbers(x, size); //<iostream> | std::cin, std::cout
    int highestNumber = findHighest(x, size);
    cout << "Highest number is " << highestNumber << std::endl;
    cout << "Program Finished " << std::endl;
}

int main()
{
    findHighestTest();

    return EXIT_SUCCESS; //<cstdlib>
}
.......



#include<iostream>
#include<vector>
#include<string>
#include<thread>
class LabTest{

     private:
          std::string testId;
          double resultValue;
     public:
          LabTest(std::string id, double result) : testId(id), resultValue(result) {}
          double getResult() const{
              return resultValue;
          }
};



//findSum of ResultValues
void findSum(const std::vector<LabTest> &tests,double& sum)
{

    for(auto &e : tests)
{
   sum += e.getResult();
}
    // return sum;
}

//findmin in 1st half ,find max in 2nd half

void findMinOfFirstHalf(const std::vector<LabTest> &tests,int& min)
{
    // int min = tests[0].getResult();
    int half =( (tests.size()-1)/2);
    for (int i = 1; i < half; i++)
    {
        if (tests[i].getResult() < min)
        {
            min = tests[i].getResult();
        }
    }


}


void findMaxOfSecondHalf(const std::vector<LabTest> &tests,int& max)
{
    //  int max = tests[0].getResult();

    for (int i = ( (tests.size()-1)/2); i < (tests.size())-1; i++)
    {
        if (tests[i].getResult() > max)
        {
            max = tests[i].getResult();
        }
    }

}

int main()
{
//create vector add 5 objects

std::vector<LabTest> tests;
 tests.emplace_back("T001",10.0);
 tests.emplace_back("T002",20.0);
 tests.emplace_back("T003",30.0);
 tests.emplace_back("T004",40.0);
 tests.emplace_back("T005",50.0);

 double sum = 0;
 int min = tests[0].getResult();
 int max = tests[0].getResult();

std::thread threadSum(findSum, std::ref(tests),std::ref(sum));
std::thread threadMin(findMinOfFirstHalf, std::ref(tests),std::ref(min));
std::thread threadMax(findMaxOfSecondHalf, std::ref(tests),std::ref(max));
threadSum.join();
threadMin.join();
threadMax.join();

//sum = findSum(tests);
std::cout << "sum is " << sum << std::endl;
//int min=findMinOfFirstHalf(tests);
std::cout << "The minimum value is: " << min << std::endl;
//int max=findMaxOfSecondHalf(tests);
std::cout << "The maximum value is: " << max << std::endl;
return 0;
}
